name: real_time_bus_tracker
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_maps_flutter: ^2.6.0
  geolocator: ^11.0.0
  geocoding: ^2.2.0
  google_sign_in: ^6.2.1
  flutter_signin_button: ^2.0.0
  firebase_core: ^2.30.0
  cloud_firestore: ^4.17.3

  firebase_auth: ^4.19.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/UI/images/back_2.jpg